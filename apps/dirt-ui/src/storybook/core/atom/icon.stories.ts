import { Meta, StoryObj } from '@storybook/angular';
import { IconComponent } from '@dirt-ui/core';

const meta: Meta<IconComponent> = {
  title: 'Core/Atom/Icon',
  component: IconComponent,
};

export default meta;

export const Default: StoryObj<IconComponent> = {};
